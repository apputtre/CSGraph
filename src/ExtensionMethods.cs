namespace CSGraph.ExtensionMethods
{
    public static class Extensions
    {
        public static void SetPriority<TElement, TPriority>(this PriorityQueue<TElement, TPriority> queue, TElement element, TPriority newPriority)
        {
            throw new NotImplementedException();
        }
    }
}